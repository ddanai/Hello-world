public class BubbleSort{
    public static int[]sort(int[]arr){
        int[]arr;
        int timeRun;
        for int i; i<timeRun-1; i++;{
            for int j; j<arr.length; j++;{
                if(arr[a]<=arr[b]){
                    exch(int a, int b){
                        int temp = arr[a];
                        arr[a] = arr[b];
                        arr[b] = temp;
                    }
                }
            }
        timeRun++;
    }
}